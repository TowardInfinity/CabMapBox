<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.map.to_in.cabmapbox">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DriverLoginLogout"
            android:label="@string/title_activity_driver_login_logout"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DriverLoginActivity" />
        <activity android:name=".CustomerLoginActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".DriverMapActivity" />
        <activity android:name=".CustomerMapActivity" />
        <activity
            android:name=".StudentLoginLogout"
            android:label="@string/title_activity_student_login_logout"
            android:theme="@style/AppTheme" />
    </application>

</manifest>